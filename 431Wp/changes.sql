CREATE TABLE Game (
                      gameID INT PRIMARY KEY AUTO_INCREMENT,
                      mode VARCHAR(50),
                      result VARCHAR(50),
                      play_history TEXT
)